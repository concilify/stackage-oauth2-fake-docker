namespace Stackage.OAuth2.Fake.Tests.Services;

using NUnit.Framework;

public class TokenGeneratorTests
{
   [Test]
   public void METHOD()
   {
      // claims
      // subject
      // scope if any scope tokens
      // refresh token if offline_access
      // cache refresh token if returned
      Assert.Fail();
   }
}
